"use strict";

var React = require('react');

var Chart = React.createClass({
  render: function() {
    return (
      <div id="vis">
        <h1>Vega-lite in ReactJS</h1>
      </div>
    );
  }
});

module.exports = Chart;
